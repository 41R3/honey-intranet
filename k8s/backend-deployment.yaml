
apiVersion: v1
kind: ServiceAccount
metadata:
  name: honey-orchestrator-sa
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: honeypot-manager
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["create", "delete", "get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: honey-orchestrator-binding
subjects:
  - kind: ServiceAccount
    name: honey-orchestrator-sa
roleRef:
  kind: Role
  name: honeypot-manager
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: honey-backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: honey-backend
  template:
    metadata:
      labels:
        app: honey-backend
    spec:
      serviceAccountName: honey-orchestrator-sa
      containers:
        - name: backend
          image: honey-intranet-backend:latest
          ports:
            - containerPort: 4000
          envFrom:
            - secretRef:
                name: honey-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: 4000
            initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: honey-backend-svc
spec:
  selector:
    app: honey-backend
  ports:
    - port: 4000
      targetPort: 4000
