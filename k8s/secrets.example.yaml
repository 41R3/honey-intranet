# Plantilla — NO subir con valores reales al repo.
apiVersion: v1
kind: Secret
metadata:
  name: honey-secrets
type: Opaque
stringData:
  DATABASE_URL: "postgresql://user:pass@host:5432/honeyintranet"
  MONGO_URL: "mongodb://host:27017"
  JWT_SECRET: "reemplazar"
